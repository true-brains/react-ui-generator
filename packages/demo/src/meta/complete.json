{
  "fields": [
    {
      "id": "email",
      "renderer": {
        "type": "text",
        "config": {
          "label": "E-mail",
          "placeholder": "Enter your e-mail"
        }
      },
      "serializer": "auth.email",
      "hidden": false
    },

    {
      "id": "password",
      "renderer": "text",
      "serializer": "auth.password"
    },

    {
      "id": "confirmation",
      "renderer": "text",
      "serializer": null
    },

    {
      "id": "sex",
      "renderer": {
        "type": "select",
        "config": {
          "label": "Sex",
          "title": "Choose your sex",
          "options": [
            { "id": 0, "title": "Female" },
            { "id": 1, "title": "Male" },
            { "id": 2, "title": "Other" }
          ]
        }
      }
    },

    {
      "id": "isMarried",
      "renderer": {
        "type": "checkbox",
        "config": {
          "label": "Married",
          "title": "Are you married?"
        }
      }
    },

    {
      "id": "aboutMe",
      "renderer": {
        "type": "textarea",
        "config": {
          "label": "Short bio",
          "placeholder": "Tell us about you...",
          "rows": 5
        }
      },
      "hidden": false
    },

    {
      "id": "answer",
      "renderer": {
        "type": "radiogroup",
        "config": {
          "label": "What is the only correct answer from this list?",
          "options": [
            { "id": 42, "title": "42" },
            { "id": 43, "title": "43" }
          ]
        }
      }
    },

    {
      "id": "relatives",
      "serializer": "auth.relatives",
      "renderer": {
        "type": "form",
        "config": {
          "label": "Add short info about your relatives",
          "fields": [
            {
              "id": "firstName",
              "renderer": {
                "type": "text",
                "config": {
                  "label": "First name",
                  "placeholder": "First name"
                }
              }
            },

            {
              "id": "lastName",
              "renderer": {
                "type": "text",
                "config": {
                  "label": "Last name",
                  "placeholder": "Last name"
                }
              }
            },

            {
              "id": "btnAddRelative",
              "renderer": {
                "type": "button",
                "config": {
                  "title": "Add",
                  "color": "dark",
                  "outline": true
                }
              },
              "actions": {
                "onClick": "addRelative"
              }
            }
          ]
        }
      }
    },

    {
      "id": "btnSend",
      "renderer": {
        "type": "button",
        "config": {
          "title": "Send",
          "color": "dark"
        }
      },
      "actions": {
        "onClick": "sendForm"
      }
    },

    {
      "id": "btnClear",
      "renderer": {
        "type": "button",
        "config": {
          "title": "Clear",
          "color": "dark",
          "outline": true
        }
      },
      "actions": {
        "onClick": "clearForm"
      }
    }
  ]
}
