import * as React from 'react';

export interface FieldsProps {
  until?: string;
}

export class Fields<P> extends React.PureComponent<FieldsProps, {}> {
  render(): JSX.Element {
    return null;
  }
}
